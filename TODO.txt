* put all characters that works as a phrase terminator in an array and test them all in the same way. Characters like . or ?
* check if string comparision can be avoided when comparing single characters.
